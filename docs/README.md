# GitHub Pages Demo

This folder contains the static GitHub Pages demo.

It does not call FastAPI, Gemini, or PostgreSQL. The GitHub Actions workflow copies `sample_data/` into this folder during deployment, then publishes the static demo.

